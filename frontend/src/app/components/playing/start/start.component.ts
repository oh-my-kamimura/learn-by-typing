import { Component } from '@angular/core';
import { RouterLink } from '@angular/router';

@Component({
  selector: 'lbt-start',
  standalone: true,
  imports: [RouterLink],
  templateUrl: './start.component.html',
  styleUrls: ['./start.component.scss', '/src/styles.scss'],
})
export class StartComponent {

}
